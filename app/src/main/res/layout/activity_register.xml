<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Header -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/primary"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="14dp">
            <TextView
                android:id="@+id/btnBack"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="← "
                android:textSize="18sp"
                android:textColor="@color/text_white"
                android:clickable="true"
                android:focusable="true"/>
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Create Account"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:textColor="@color/text_white"/>
                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Smart Library Registration"
                    android:textSize="10sp"
                    android:textColor="#b0bcd8"/>
            </LinearLayout>
        </LinearLayout>

        <!-- Form fields -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingLeft="14dp"
            android:paddingRight="14dp">

            <TextView style="@style/FieldLabel" android:text="Full Name *"/>
            <EditText
                android:id="@+id/etFullName"
                style="@style/LibraryInput"
                android:hint="@string/hint_fullname"
                android:inputType="textCapWords"
                android:maxLines="1"/>

            <TextView style="@style/FieldLabel" android:text="Email Address *"/>
            <EditText
                android:id="@+id/etEmail"
                style="@style/LibraryInput"
                android:hint="@string/hint_email"
                android:inputType="textEmailAddress"
                android:maxLines="1"/>

            <TextView style="@style/FieldLabel" android:text="Password * (min. 8 chars)"/>
            <EditText
                android:id="@+id/etPassword"
                style="@style/LibraryInput"
                android:hint="@string/hint_password"
                android:inputType="textPassword"
                android:maxLines="1"/>

            <TextView style="@style/FieldLabel" android:text="Confirm Password *"/>
            <EditText
                android:id="@+id/etConfirmPassword"
                style="@style/LibraryInput"
                android:hint="@string/hint_confirm_password"
                android:inputType="textPassword"
                android:maxLines="1"/>

            <TextView style="@style/FieldLabel" android:text="Phone Number *"/>
            <EditText
                android:id="@+id/etPhone"
                style="@style/LibraryInput"
                android:hint="@string/hint_phone"
                android:inputType="phone"
                android:maxLines="1"/>

            <TextView style="@style/FieldLabel" android:text="Role"/>
            <Spinner
                android:id="@+id/spinRole"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_input"
                android:layout_marginTop="3dp"
                android:entries="@array/user_roles"/>

        </LinearLayout>

        <!-- Status box -->
        <LinearLayout
            android:id="@+id/layoutStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_result"
            android:layout_marginLeft="14dp"
            android:layout_marginRight="14dp"
            android:layout_marginTop="8dp"
            android:padding="8dp"
            android:visibility="gone">
            <TextView
                android:id="@+id/tvStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="11sp"
                android:textColor="@color/success"/>
        </LinearLayout>

        <!-- Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingLeft="14dp"
            android:paddingRight="14dp"
            android:paddingTop="12dp"
            android:layout_gravity="center"
            android:weightSum="2">
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnRegister"
                style="@style/BtnSuccess"
                android:text="@string/btn_register"/>
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnReset"
                style="@style/BtnGrey"
                android:text="@string/btn_reset"/>
        </LinearLayout>

        <TextView
            android:id="@+id/tvLoginLink"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/link_login"
            android:textSize="11sp"
            android:textColor="@color/accent"
            android:gravity="center"
            android:padding="10dp"
            android:clickable="true"
            android:focusable="true"/>

    </LinearLayout>
</ScrollView>
